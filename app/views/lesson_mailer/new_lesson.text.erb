A new lesson was created for <%= @student.firstname %>, at <%= @lesson.lesson_date.strftime('%a, %B %e, %l:%M %p ')%>.

<%= @lesson.reminder %>

To view this lesson, visit the student's page:

<%= student_url(@student) %>